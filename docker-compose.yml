services:
  mock-api:
    build: .
    container_name: disputeos-mock-api
    ports:
      - "8888:8888"
    volumes:
      - ./main.py:/app/main.py
      - ./generators.py:/app/generators.py
    command: uvicorn main:app --host 0.0.0.0 --port 8888 --reload
    restart: unless-stopped
